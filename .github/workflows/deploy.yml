name: Deploy

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment status
        run: |
          echo "Deployment workflow is intentionally scaffolded only."
          echo "Add Cloudflare authentication, secrets, and wrangler deploy steps when deployment is ready."
